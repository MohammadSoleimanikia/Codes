// methods 
console.log(Math.round(40.6));
console.log(Math.floor(20.9));
console.log(Math.ceil(70.2));
// random between 0.0 to 0.9
console.log(Math.random());
// if we want a random number between two number we use this 
let randNum=Math.floor(Math.random()*6)// between 0 and 6 (bellow the 6) add one to include six
console.log('random number is '+randNum);
// modulo operator 
console.log(12%5);

// NAN not a number
0/0==NaN;

// parse string to float
parseFloat('20.45446');//20.45446
// parse string to integer 
parseInt('20.5454')//20
